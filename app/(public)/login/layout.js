
export const metadata = {
    title: "Login | Fakhri IT Services",
    description: "Access your dashboard.",
    robots: {
        index: false,
        follow: false,
    },
};

export default function LoginLayout({ children }) {
    return children;
}
