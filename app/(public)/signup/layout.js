
export const metadata = {
    title: "Sign Up | Fakhri IT Services",
    description: "Create an account.",
    robots: {
        index: false,
        follow: false,
    },
};

export default function SignupLayout({ children }) {
    return children;
}
